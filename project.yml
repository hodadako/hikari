name: Hikari
options:
  bundleIdPrefix: com.hodadako
  deploymentTarget:
    macOS: "15.0"
  groupSortPosition: top
  generateEmptyDirectories: true
settings:
  base:
    SWIFT_VERSION: "5.9"
    MACOSX_DEPLOYMENT_TARGET: "15.0"
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: ""
    ENABLE_HARDENED_RUNTIME: YES
    CURRENT_PROJECT_VERSION: 12
    MARKETING_VERSION: 0.3.2
targets:
  LuminaCore:
    type: framework.static
    platform: macOS
    sources:
      - Sources/LuminaCore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.hodadako.LuminaCore
        GENERATE_INFOPLIST_FILE: YES
        DEFINES_MODULE: YES
  LuminaNativeLock:
    type: framework.static
    platform: macOS
    deploymentTarget: "15.0"
    sources:
      - Sources/LuminaNativeLock
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.hodadako.LuminaNativeLock
        GENERATE_INFOPLIST_FILE: YES
        DEFINES_MODULE: YES
  LuminaNativeTool:
    type: tool
    platform: macOS
    deploymentTarget: "15.0"
    sources:
      - Sources/LuminaNativeTool
    dependencies:
      - target: LuminaNativeLock
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.hodadako.Lumina.NativeTool
        PRODUCT_NAME: hikari-native-tool
        MACOSX_DEPLOYMENT_TARGET: "15.0"
        SKIP_INSTALL: YES
  Hikari:
    type: application
    platform: macOS
    productName: Hikari
    deploymentTarget: "15.0"
    sources:
      - path: Sources/LuminaApp
        excludes:
          - LockShortcutController.swift
          - ScreenSaverInstaller.swift
      - path: Resources/Assets.xcassets
      - path: Resources/en.lproj
        buildPhase: resources
      - path: Resources/ko.lproj
        buildPhase: resources
    info:
      path: Resources/Hikari-Info.plist
      properties:
        CFBundleDisplayName: Hikari
        CFBundleName: Hikari
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        LSApplicationCategoryType: public.app-category.utilities
        LSMinimumSystemVersion: $(MACOSX_DEPLOYMENT_TARGET)
        LSUIElement: true
        NSHighResolutionCapable: true
        NSHumanReadableCopyright: Copyright © 2026 Hikari contributors
    dependencies:
      - target: LuminaCore
      - target: LuminaNativeLock
      - target: LuminaNativeTool
        embed: false
    postBuildScripts:
      - name: Embed Hikari Native Tool
        script: |
          set -euo pipefail
          destination="${TARGET_BUILD_DIR}/${CONTENTS_FOLDER_PATH}/MacOS"
          mkdir -p "${destination}"
          ditto "${BUILT_PRODUCTS_DIR}/hikari-native-tool" \
            "${destination}/hikari-native-tool"
        basedOnDependencyAnalysis: false
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.hodadako.Lumina.NativeLocal
        PRODUCT_NAME: Hikari
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.utilities
        MACOSX_DEPLOYMENT_TARGET: "15.0"
  LuminaCoreTests:
    type: bundle.unit-test
    platform: macOS
    deploymentTarget: "15.0"
    sources:
      - Tests/LuminaCoreTests
    dependencies:
      - target: LuminaCore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.hodadako.LuminaCoreTests
  LuminaNativeLockTests:
    type: bundle.unit-test
    platform: macOS
    deploymentTarget: "15.0"
    sources:
      - Tests/LuminaNativeLockTests
    dependencies:
      - target: LuminaNativeLock
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.hodadako.LuminaNativeLockTests
schemes:
  Hikari:
    build:
      targets:
        Hikari: all
        LuminaNativeTool: all
        LuminaCoreTests: [test]
        LuminaNativeLockTests: [test]
    test:
      targets:
        - LuminaCoreTests
        - LuminaNativeLockTests
      gatherCoverageData: true
